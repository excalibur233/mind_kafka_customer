const config = {
    // db_uri: "mongodb://username:password@host:port/database"
    db_uri: "mongodb://username:password@host:port/database",

    kafka_host: "",
    kafka_topic: "",
};

module.exports = config;
