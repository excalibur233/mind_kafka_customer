const config = {
    // db_uri: "mongodb://username:password@host:port/database"
    db_uri: "mongodb://localhost/test_database",

    kafka_host: "localhost:2181",
    kafka_topic: "test",
};

module.exports = config;
